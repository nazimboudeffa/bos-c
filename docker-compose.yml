services:
  build:
    build: .
    container_name: bos-builder
    volumes:
      - .:/bos
    command: make dist

  clean:
    build: .
    container_name: bos-cleaner
    volumes:
      - .:/bos
    command: make clean
